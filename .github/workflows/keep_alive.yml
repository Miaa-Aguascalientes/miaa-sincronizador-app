name: Despierta Streamlit

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: |
          curl -I https://miaa-sincronizador-ver-app.streamlit.app/
          echo "Ping enviado con éxito"
